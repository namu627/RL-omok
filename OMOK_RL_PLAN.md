# 오목 Self-Play 강화학습 프로젝트 계획서

> 규칙(승패 판정)만 주고, 인간 기보 없이 self-play로 스스로 강해지는 오목 AI를 만든다.
> 최종 목적지는 AlphaZero식(MCTS + 신경망) 구조이며, RL 원리를 단계적으로 체득하면서 약→강 성장 과정을 측정 가능한 형태로 시연한다.

---

## 0. 목표 우선순위

| 순위 | 목표 | 어떻게 충족되는가 |
|------|------|------------------|
| 1 | RL 원리 학습 | 테이블 Q-러닝부터 직접 구현 → 벨만 방정식·탐험vs활용·credit assignment를 손으로 체득 |
| 2 | 진짜 강한 AI | 최종 단계에서 AlphaZero식 self-play (MCTS + 신경망) 구현 |
| 3 | 약→강 성장 시연 | 모든 단계에서 "고정 상대 대비 승률 곡선"을 기록·시각화 |

세 목표는 충돌하지 않고 **하나의 로드맵 위에 단계적으로 쌓인다.**

---

## 1. 기술 스택

- **언어/라이브러리**: Python 3.10+, NumPy (1~2단계), PyTorch (3단계 이후)
- **시각화**: matplotlib (승률 곡선), 선택적으로 TensorBoard
- **환경**: 입문은 로컬 CPU로 충분. 보드 확장·신경망 학습 단계에서 Google Colab 무료 GPU 활용
- **시연용(선택)**: 학습 완료 모델을 기존 Vanilla JS 오목 웹에 연결해 브라우저에서 대국
- **버전 관리**: git (각 단계를 브랜치/태그로 구분)

---

## 2. 단계별 로드맵

### Phase 0 — 환경(Environment) 구축
**목표**: 학습 알고리즘과 완전히 분리된, 재사용 가능한 오목 환경.

- 보드 크기 파라미터화 (`board_size`), 승리 조건 파라미터화 (`n_in_row`)
- 입문은 **6x6 보드, 4목 승리**로 시작 (상태 수가 작아 테이블 방식이 통함)
- 핵심 인터페이스 (OpenAI Gym 스타일):
  - `reset()` → 초기 상태
  - `step(action)` → (next_state, reward, done, info)
  - `legal_actions(state)` → 둘 수 있는 자리 목록
  - `render()` → 콘솔/이미지 출력
- 보상 설계: 승 +1 / 패 -1 / 무 0 / 진행 중 0
- **검증**: 사람이 직접 둬보거나 랜덤 vs 랜덤 대국이 규칙대로 끝나는지 테스트

**산출물**: `env/gomoku.py`, 테스트 코드

---

### Phase 1 — 테이블 Q-러닝 (RL 원리 체득)
**목표**: 신경망 없이 RL의 뼈대를 직접 손으로 구현.

- 상태를 키로 하는 Q-테이블(dict) 구성
- 핵심 요소:
  - **벨만 업데이트**: `Q(s,a) ← Q(s,a) + α[r + γ·max Q(s',a') − Q(s,a)]`
  - **ε-greedy**: 탐험(exploration) vs 활용(exploitation) 균형
  - **self-play**: 같은 Q-테이블을 두 플레이어가 공유하거나, 번갈아 학습
- 2인 대결 특성 처리: 상대 시점에서는 보상 부호 반전 (zero-sum)
- **성장 측정 훅(중요)**: 100판마다 "고정 랜덤 상대와 N판 대국 → 승률" 기록

**배우는 개념**: 벨만 방정식, credit assignment, 탐험vs활용, zero-sum self-play
**산출물**: `agents/q_learning.py`, 승률 곡선 그래프

---

### Phase 2 — Policy Gradient / Actor-Critic (정책 기반 입문)
**목표**: 가치 기반(Q)과 다른 축인 정책 기반 학습을 체험.

- REINFORCE: 한 판 종료 후 이긴 판의 수 확률↑ / 진 판의 수 확률↓
- 분산 문제 체감 → baseline(가치함수) 도입 → **Actor-Critic** 구조로 발전
- 작은 보드에서 NumPy 또는 간단한 PyTorch MLP로 구현 가능

**배우는 개념**: 정책 경사, 분산 감소, Actor-Critic
**산출물**: `agents/policy_gradient.py`, 두 접근(Q vs PG) 승률 비교 그래프

> Phase 1, 2를 통해 "학습이 실제로 일어난다"는 것을 작은 보드에서 확실히 검증한 뒤 Phase 3로.

---

### Phase 3 — AlphaZero 축소판 (MCTS + 신경망) ★핵심
**목표**: "진짜 강한 AI"의 본체. 규칙만으로 self-play하며 스스로 강해지는 루프.

- **신경망**: 입력=보드 상태, 출력=정책 π(어디가 유망) + 가치 v(이길 판인가)
  - 작은 CNN (보드를 이미지처럼 처리)
- **MCTS**: 신경망의 정책/가치로 안내되는 트리 탐색 (PUCT 공식)
- **학습 루프**:
  1. 현재 신경망 + MCTS로 self-play 대국 → (상태, MCTS 방문분포, 최종승패) 수집
  2. 그 데이터로 신경망 학습 (정책은 MCTS 분포에, 가치는 실제 승패에 맞춤)
  3. 강해진 신경망으로 다시 self-play → 반복
- **보드 확장**: 6x6 → 9x9 → 13x13/15x15 (이 단계부터 Colab GPU 권장)

**배우는 개념**: MCTS(PUCT), 정책+가치 네트워크, self-play 학습 루프, 신경망 일반화
**산출물**: `agents/alphazero/` (네트워크, MCTS, 학습 루프), 체크포인트, 세대별 대결 결과

---

### Phase 4 — 강화 및 시연 (선택/확장)
- 세대 간 대결 토너먼트로 실력 향상 정량화 (Elo 추정)
- 학습 곡선·세대별 기보 시각화
- 학습된 모델을 웹(JS)에 연결해 사람이 직접 대국하는 데모
- 하이퍼파라미터 튜닝, 학습 가속(병렬 self-play 등)

---

## 3. 성장 시연을 위한 측정 설계 (전 단계 공통)

약→강을 "느낌"이 아니라 **데이터**로 보여주는 것이 이 프로젝트의 핵심 가치.

- **고정 기준 상대 대비 승률 곡선**: 학습 X판마다 랜덤 봇(또는 간단한 규칙 봇)과 평가전 → 승률 기록 → 우상향 그래프
- **세대 간 대결**: N판 학습 모델 vs 2N판 학습 모델 → 후자가 이기는 비율
- **정성적 기록**: 특정 학습량 시점의 대국 기보를 저장해 "이 수를 둘 줄 알게 된 시점" 추적

---

## 4. Claude Code 작업 진행 순서

1. 프로젝트 폴더/구조 초기화, git init
2. **Phase 0**: 환경 구현 + 테스트 → 사람이 직접 둬보며 검증
3. **Phase 1**: 테이블 Q-러닝 + 승률 측정 훅 → 그래프로 학습 확인
4. **Phase 2**: Policy Gradient → Q 방식과 비교
5. **Phase 3**: AlphaZero 축소판 (네트워크 → MCTS → 학습 루프 순서로)
6. **Phase 4**: 보드 확장 + 시연/데모

> 각 Phase는 **독립 실행·검증 가능**하게 만든다. 다음으로 넘어가기 전 반드시 "성장 곡선"으로 학습이 일어남을 확인한다.

---

## 5. 권장 디렉터리 구조

```
omok-rl/
├── env/
│   └── gomoku.py          # Phase 0: 환경
├── agents/
│   ├── random_agent.py    # 평가용 고정 상대
│   ├── q_learning.py      # Phase 1
│   ├── policy_gradient.py # Phase 2
│   └── alphazero/         # Phase 3
│       ├── network.py
│       ├── mcts.py
│       └── trainer.py
├── eval/
│   └── arena.py           # 승률 측정·세대 대결
├── viz/
│   └── plot_winrate.py    # 성장 곡선 시각화
├── checkpoints/           # 학습된 모델 저장
├── scripts/               # 학습/평가 실행 스크립트
└── README.md
```

---

## 6. 현실적 유의사항

- **작은 보드부터**: 6x6/4목으로 시작해야 학습이 빠르고 약→강이 또렷이 보임. 처음부터 15x15는 금물.
- **연산량**: "몇만 번 학습"은 self-play 수만 판을 의미. Phase 1~2는 CPU로 충분, Phase 3 대형 보드는 GPU(Colab) 권장.
- **단계적 검증**: 복잡한 AlphaZero를 먼저 짜면 버그 위치 파악이 어려움. 단순 단계에서 "학습이 된다"를 먼저 확인.
- **2인 zero-sum 처리 주의**: 상대 시점 보상 부호 반전 등, 1인 게임 RL과 다른 부분을 명확히.
