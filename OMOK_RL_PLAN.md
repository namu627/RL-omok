# 오목 Self-Play 강화학습 프로젝트 계획서 (v2)

> 규칙(승패 판정)만 주고, 인간 기보 없이 self-play로 스스로 강해지는 오목 AI를 만든다.
> 최종 목적지는 **정통 렌주룰(連珠) 15×15 오목**을 두는 AlphaZero식(MCTS + 신경망) AI다.
> RL 원리를 단계적으로 체득하면서 약→강 성장 과정을 측정 가능한 형태로 시연한다.

---

## 0. 목표 우선순위

| 순위 | 목표 | 어떻게 충족되는가 |
|------|------|------------------|
| 1 | RL 원리 학습 | 테이블 Q-러닝부터 직접 구현 → 벨만 방정식·탐험vs활용·credit assignment 체득 |
| 2 | 진짜 강한 AI | AlphaZero식 self-play (MCTS + 신경망)로 정통 렌주룰 AI 완성 |
| 3 | 약→강 성장 시연 | 모든 단계에서 "고정 상대 대비 승률 곡선" 기록·시각화 |

**최종 룰 목표: 정통 렌주룰** — 흑(선공)에게만 금수(33 / 44 / 장목)를 부과해 선공 이점을 억제하는 대회 규칙. 백은 제약 없음.

---

## 1. 진행 현황

| Phase | 내용 | 상태 |
|-------|------|------|
| Phase 0 | 환경 구축 (6×6 / 4목 자유룰) | ✅ 완료 |
| Phase 1 | 테이블 Q-러닝 + ε-greedy self-play + 승률 곡선 | ✅ 완료 |
| Phase 2 | Policy Gradient / Actor-Critic | ✅ 완료 |
| Phase 3 | AlphaZero 축소판 (MCTS + 신경망) | ✅ 완료 (6×6 수렴, 9×9 확장 검증) |
| Phase 4a | 정통 렌주룰 금수 판정 (1.5단계) + 15×15 | 🔄 진행 중 — 금수 판정 1.5단계 완료, 환경 통합 단계 |
| Phase 4b | swap 개식 (보류 — 행동공간 재설계 필요) | ⏸ 보류 |

**Phase 2 결과**: REINFORCE 98.0% / Actor-Critic 95.7% vs Q-Learning 54.7% — 함수근사(파라미터 공유)가 테이블의 재방문 한계를 넘어섬을 입증.
**Phase 3 결과**: 6×6/4목 — iter40 vs iter20 아레나 100%(학습 입증), iter60 vs iter40 50%(수렴). 9×9/5목 — iter20 랜덤봇 100%(보드 확장에도 학습 신호 확인). 파이프라인 검증 완료, 렌주룰에 그대로 재사용.

**Phase 0 결과**: reset/step/불법착수, 가로·세로·대각선 4목 감지 전부 PASS. 랜덤 1000판 흑 55.2% / 백 44.1% / 무 0.7% (선공 비대칭이 현실적으로 반영됨 → 환경 정상).

> Phase 1~3은 **단순한 자유룰(먼저 N개 잇기)**로 진행해 학습 파이프라인을 깨끗하게 검증한다.
> 렌주룰은 파이프라인이 검증된 **Phase 4에서 정식 도입**한다 (이유는 §6 참조).

---

## 2. 기술 스택

- **언어/라이브러리**: Python 3.10+, NumPy (Phase 1~2), PyTorch (Phase 3 이후)
- **시각화**: matplotlib (승률 곡선), 선택적 TensorBoard
- **환경**: Phase 1~2는 로컬 CPU로 충분. Phase 3 대형 보드·신경망부터 Google Colab 무료 GPU 권장
- **시연(선택)**: 학습 모델을 기존 Vanilla JS 오목 웹에 연결해 브라우저 대국
- **버전 관리**: git (각 Phase를 브랜치/태그로 구분)

---

## 3. 남은 단계 로드맵

### Phase 2 — Policy Gradient / Actor-Critic (정책 기반 입문)
**목표**: 가치 기반(Q)과 다른 축인 정책 기반 학습 체험.

- REINFORCE: 한 판 종료 후 이긴 판의 수 확률↑ / 진 판의 수 확률↓
- 분산 문제 체감 → baseline(가치함수) 도입 → **Actor-Critic**으로 발전
- 작은 보드에서 NumPy 또는 간단한 PyTorch MLP로 구현
- **성장 측정**: 100판마다 random_agent와 평가전 → 승률 기록

**배우는 개념**: 정책 경사, 분산 감소, Actor-Critic
**산출물**: `agents/policy_gradient.py`, Q vs PG 승률 비교 그래프

---

### Phase 3 — AlphaZero 축소판 (MCTS + 신경망) ★핵심
**목표**: "진짜 강한 AI"의 본체. 규칙만으로 self-play하며 스스로 강해지는 루프. **이 파이프라인이 곧 최종 자산** — 룰이 바뀌어도 그대로 재사용된다.

- **신경망**: 입력=보드 상태, 출력=정책 π(유망한 수) + 가치 v(승률 평가). 작은 CNN.
- **MCTS**: 신경망의 정책/가치로 안내되는 트리 탐색 (PUCT 공식)
- **학습 루프**:
  1. 현재 신경망 + MCTS로 self-play → (상태, MCTS 방문분포, 최종승패) 수집
  2. 그 데이터로 신경망 학습 (정책→MCTS 분포, 가치→실제 승패)
  3. 강해진 신경망으로 다시 self-play → 반복
- 여전히 **자유룰**로 진행하며 보드는 6×6 → 9×9 정도까지 (검증 목적)

**배우는 개념**: MCTS(PUCT), 정책+가치 네트워크, self-play 학습 루프
**산출물**: `agents/alphazero/` (network, mcts, trainer), 체크포인트, 세대별 대결 결과

---

### Phase 4a — 정통 렌주룰 금수 완전판 + 15×15 ★최종 목표
**목표**: Phase 3에서 검증된 파이프라인 위에 정통 렌주룰 환경을 연결해 **0부터 새로 self-play 학습**. swap 개식은 제외(4b로 보류), 금수는 재귀 예외 포함 완전판으로 구현.

**핵심 원칙: 학습 알고리즘은 그대로, 환경만 교체한다.**
Phase 3의 네트워크·MCTS·trainer는 룰에 종속되지 않으므로 재사용한다. 렌주룰은 `env`의 승패 판정·합법 수 목록에만 들어간다. 가중치를 잇지 않고 같은 파이프라인에 렌주룰 환경을 붙여 self-play를 0부터 다시 돌린다.

#### 금수 규칙의 정확한 정의 (흑에게만 적용 — 구현 전 반드시 합의)
- **5목 우선 원칙 (최우선)**: 흑의 한 수가 정확히 5목을 완성하면, 그 수가 동시에 금수 조건(장목/44 등)을 만족해도 **승리로 처리**한다. 금수 판정보다 5목 완성이 우선.
- **장목(overline)**: 흑이 6목 이상을 만들면 금수(패배). 백은 6목 이상도 승리.
- **44 금수**: 한 수로 "4"(한 수 더 두면 5가 되는 형태, 열린/닫힌 무관)를 2개 이상 동시 생성.
- **33 금수**: 한 수로 "열린 3"을 2개 이상 동시 생성.
  - 열린 3 = 한 수 더 두면 **열린 4**가 되는 3.
  - **재귀 예외(핵심 난점)**: 어떤 3을 열린 4로 만드는 수가 그 자체로 금수(33/44)라 둘 수 없다면, 그 3은 진짜 열린 3이 아니다 → 33 카운트에서 제외. 33 판정이 다시 금수 판정을 호출하는 재귀 구조.

#### 구현 순서 (테스트 우선 — 절대 학습부터 붙이지 않는다)
1. **단위 테스트 케이스 설계 먼저**: 장목/44/33(재귀 예외 포함)/5목 우선/정상 합법수의 알려진 보드 패턴을 정답 라벨과 함께 작성. 정상수(금수 아님) 케이스도 반드시 포함(거짓양성 방지).
2. `env/renju_rules.py` — 금수 판정 모듈 구현. 위 테스트를 전부 통과할 때까지 학습은 시작하지 않는다.
3. `env/gomoku.py`에 `renju=True` 옵션으로 통합 (15×15, n_in_row=5). 흑 금수 시 즉시 패배, 백 무제약.
4. 환경이 검증된 뒤에야 Phase 3 파이프라인 연결 → self-play 0부터 학습.

> ⚠️ **금수 판정 버그는 잘못된 규칙을 그대로 학습시킨다.** 학습 곡선만으론 환경 버그인지 알고리즘 문제인지 구분 불가 → 환경 정확성이 최우선. 그래서 테스트가 먼저다.

#### 금수 판정 구현 현황 & 알려진 한계 (2026-06 기준)
테스트 40개 전부 통과. 단계적 구현 결과 현재 **"1.5단계"** 수준에 도달:
- ✅ 장목(`is_forbidden_overline`), 44(`is_forbidden_44`), 비재귀 33(`is_forbidden_33_nonrecursive`) — 완성.
- ✅ 1단계 재귀(`is_forbidden_33_recursive`): 열린 3의 **완성 지점이 44/장목 금수이면 제외, 하나라도 합법이면 유지**. R-1(완성 지점 전부 금수→합법)·R-2(하나만 금수→33성립) 양방향 검증 완료.
- ⚠️ **알려진 한계 — 완성 지점이 33금수인 경우 미처리**: 현재 `is_forbidden_33_recursive`는 완성 지점의 금수를 44/장목만 검사하고, 그 지점이 33금수인 경우는 "합법"으로 간주한다(이름과 달리 실제 재귀 호출이 없는 "1.5단계"). 사이클 방지(visited)도 없음.
  - **영향**: 완성 지점이 44·장목이 아니면서 정확히 33금수인 배치는 실제 대국에서 극히 드물어, self-play 학습에 미치는 영향은 사실상 무시 가능.
  - **결정**: 학습을 우선해 1.5단계로 환경 통합 진행. 완전 재귀(완성 지점의 33 재귀 호출 + visited 사이클 방지)는 **향후 과제(Phase 4a-후속)**로 남김. 완전판이 필요해지면 깊은 재귀 케이스 + 사이클 케이스를 추가해 검증한 뒤 확장.

#### 학습 & 시연
- 검증된 렌주룰 환경 + Phase 3 파이프라인으로 self-play 0부터 학습. 보드가 커서 self-play 판수·연산이 크게 늘어남 → Colab GPU 권장(성능이 아니라 시간·자원 이슈).
- 세대 간 토너먼트로 실력 정량화, 학습 곡선·기보 시각화, 학습 모델을 웹(JS)에 연결해 사람이 직접 대국하는 데모.

**산출물**: `env/renju_rules.py` + 단위 테스트, 렌주룰 통합 환경, 학습된 15×15 렌주 모델, 데모

### Phase 4b — swap 개식 (보류)
정통 대회 개식(RIF/Taraguchi/Soosõrv-8 등)은 "돌 놓기"가 아니라 "배치 제안·흑백 선택" 메타 결정이 필요해 행동공간을 재설계해야 한다. 4a가 완전히 동작한 뒤 별도 단계로 다룬다.

---

## 4. 성장 시연을 위한 측정 설계 (전 단계 공통)

- **고정 기준 상대 대비 승률 곡선**: 학습 X판마다 랜덤/규칙 봇과 평가전 → 승률 기록 → 우상향 그래프
- **세대 간 대결**: N판 학습 모델 vs 2N판 학습 모델 → 후자 승률
- **정성적 기록**: 특정 학습량 시점의 기보 저장 → "이 수를 둘 줄 알게 된 시점" 추적

---

## 5. 권장 디렉터리 구조

```
omok-rl/
├── env/
│   ├── gomoku.py          # 환경 (board_size, n_in_row, renju 옵션)
│   └── renju_rules.py     # Phase 4: 금수(33/44/장목) 판정 + 단위 테스트
├── agents/
│   ├── random_agent.py    # 평가용 고정 상대
│   ├── q_learning.py      # Phase 1 ✅
│   ├── policy_gradient.py # Phase 2
│   └── alphazero/         # Phase 3 (Phase 4에서 그대로 재사용)
│       ├── network.py
│       ├── mcts.py
│       └── trainer.py
├── eval/
│   └── arena.py           # 승률 측정·세대 대결
├── viz/
│   └── plot_winrate.py    # 성장 곡선 시각화
├── checkpoints/
├── scripts/
└── README.md
```

---

## 6. 현실적 유의사항

- **작은 자유룰부터, 렌주룰은 마지막**: Phase 1~3은 단순 룰로 "학습이 실제로 일어나는가"를 깨끗이 검증. 렌주룰을 먼저 넣으면 환경 복잡도 + 학습 난이도가 동시에 올라가 디버깅이 지옥이 됨.
- **렌주룰의 리스크는 학습 성능이 아니라 환경 정확성**: 금수 판정(특히 33)이 까다로움. 별도 모듈 + 단위 테스트 우선으로 환경을 먼저 검증한 뒤 self-play를 붙인다.
- **렌주룰 모델은 0부터 재학습**: Phase 3 파이프라인은 재사용하되 가중치를 잇지 않는다.
- **자원**: 15×15 렌주 학습은 self-play 수만~수십만 판 + GPU(Colab) 권장. 성능이 아니라 시간·자원 이슈.
- **2인 zero-sum 처리**: 상대 시점 보상 부호 반전 등 1인 게임 RL과 다른 부분을 명확히.
