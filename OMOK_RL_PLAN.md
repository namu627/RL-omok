# 오목 Self-Play 강화학습 프로젝트 계획서 (v2)

> 규칙(승패 판정)만 주고, 인간 기보 없이 self-play로 스스로 강해지는 오목 AI를 만든다.
> 최종 목적지는 **정통 렌주룰(連珠) 15×15 오목**을 두는 AlphaZero식(MCTS + 신경망) AI다.
> RL 원리를 단계적으로 체득하면서 약→강 성장 과정을 측정 가능한 형태로 시연한다.

---

## 0. 목표 우선순위

| 순위 | 목표 | 어떻게 충족되는가 |
|------|------|------------------|
| 1 | RL 원리 학습 | 테이블 Q-러닝부터 직접 구현 → 벨만 방정식·탐험vs활용·credit assignment 체득 |
| 2 | 진짜 강한 AI | AlphaZero식 self-play (MCTS + 신경망)로 정통 렌주룰 AI 완성 |
| 3 | 약→강 성장 시연 | 모든 단계에서 "고정 상대 대비 승률 곡선" 기록·시각화 |

**최종 룰 목표: 정통 렌주룰** — 흑(선공)에게만 금수(33 / 44 / 장목)를 부과해 선공 이점을 억제하는 대회 규칙. 백은 제약 없음.

---

## 1. 진행 현황

| Phase | 내용 | 상태 |
|-------|------|------|
| Phase 0 | 환경 구축 (6×6 / 4목 자유룰) | ✅ 완료 |
| Phase 1 | 테이블 Q-러닝 + ε-greedy self-play + 승률 곡선 | ✅ 완료 |
| Phase 2 | Policy Gradient / Actor-Critic | ⬜ 예정 |
| Phase 3 | AlphaZero 축소판 (MCTS + 신경망) | ⬜ 예정 |
| Phase 4 | 보드 확장 + **정통 렌주룰** + 시연 | ⬜ 예정 |

**Phase 0 결과**: reset/step/불법착수, 가로·세로·대각선 4목 감지 전부 PASS. 랜덤 1000판 흑 55.2% / 백 44.1% / 무 0.7% (선공 비대칭이 현실적으로 반영됨 → 환경 정상).

> Phase 1~3은 **단순한 자유룰(먼저 N개 잇기)**로 진행해 학습 파이프라인을 깨끗하게 검증한다.
> 렌주룰은 파이프라인이 검증된 **Phase 4에서 정식 도입**한다 (이유는 §6 참조).

---

## 2. 기술 스택

- **언어/라이브러리**: Python 3.10+, NumPy (Phase 1~2), PyTorch (Phase 3 이후)
- **시각화**: matplotlib (승률 곡선), 선택적 TensorBoard
- **환경**: Phase 1~2는 로컬 CPU로 충분. Phase 3 대형 보드·신경망부터 Google Colab 무료 GPU 권장
- **시연(선택)**: 학습 모델을 기존 Vanilla JS 오목 웹에 연결해 브라우저 대국
- **버전 관리**: git (각 Phase를 브랜치/태그로 구분)

---

## 3. 남은 단계 로드맵

### Phase 2 — Policy Gradient / Actor-Critic (정책 기반 입문)
**목표**: 가치 기반(Q)과 다른 축인 정책 기반 학습 체험.

- REINFORCE: 한 판 종료 후 이긴 판의 수 확률↑ / 진 판의 수 확률↓
- 분산 문제 체감 → baseline(가치함수) 도입 → **Actor-Critic**으로 발전
- 작은 보드에서 NumPy 또는 간단한 PyTorch MLP로 구현
- **성장 측정**: 100판마다 random_agent와 평가전 → 승률 기록

**배우는 개념**: 정책 경사, 분산 감소, Actor-Critic
**산출물**: `agents/policy_gradient.py`, Q vs PG 승률 비교 그래프

---

### Phase 3 — AlphaZero 축소판 (MCTS + 신경망) ★핵심
**목표**: "진짜 강한 AI"의 본체. 규칙만으로 self-play하며 스스로 강해지는 루프. **이 파이프라인이 곧 최종 자산** — 룰이 바뀌어도 그대로 재사용된다.

- **신경망**: 입력=보드 상태, 출력=정책 π(유망한 수) + 가치 v(승률 평가). 작은 CNN.
- **MCTS**: 신경망의 정책/가치로 안내되는 트리 탐색 (PUCT 공식)
- **학습 루프**:
  1. 현재 신경망 + MCTS로 self-play → (상태, MCTS 방문분포, 최종승패) 수집
  2. 그 데이터로 신경망 학습 (정책→MCTS 분포, 가치→실제 승패)
  3. 강해진 신경망으로 다시 self-play → 반복
- 여전히 **자유룰**로 진행하며 보드는 6×6 → 9×9 정도까지 (검증 목적)

**배우는 개념**: MCTS(PUCT), 정책+가치 네트워크, self-play 학습 루프
**산출물**: `agents/alphazero/` (network, mcts, trainer), 체크포인트, 세대별 대결 결과

---

### Phase 4 — 정통 렌주룰 + 15×15 확장 + 시연 ★최종 목표
**목표**: Phase 3에서 검증된 파이프라인 위에, 정통 렌주룰 환경을 연결해 **0부터 새로 self-play 학습**. 진짜 오목 AI 완성.

**핵심 원칙: 학습 알고리즘은 그대로, 환경만 교체한다.**
Phase 3의 네트워크·MCTS·trainer는 룰에 종속되지 않으므로 재사용한다. 렌주룰은 `env`의 승패 판정·합법 수 목록에만 들어간다. 기존 4목 모델 가중치를 잇지 않고, 같은 파이프라인에 렌주룰 환경을 붙여 self-play를 0부터 다시 돌린다 (AlphaZero가 바둑/체스/쇼기를 각각 0부터 학습한 것과 동일 원리).

**4-1. 렌주룰 환경 구현 (가장 신경 써야 할 부분)**
- 보드 15×15, `n_in_row=5`, `renju=True` 옵션
- 흑 금수 판정을 **별도 모듈** `env/renju_rules.py`로 분리:
  - **33 금수**: 한 수로 "열린 3"을 동시에 2개 생성 (열린 3의 정의·재귀 예외 주의)
  - **44 금수**: 한 수로 "4"를 동시에 2개 생성
  - **장목 금수**: 6목 이상(정확히 5를 초과)
  - 흑이 금수를 두면 즉시 패배. 백은 모든 제약 없음(6목 이상도 승리).
- **단위 테스트 우선**: 학습 붙이기 전에 알려진 33/44/장목 패턴들을 정답과 함께 빡세게 테스트. ⚠️ 금수 판정 버그는 잘못된 규칙을 그대로 학습시키며, 학습 곡선만으론 환경 버그인지 알고리즘 문제인지 구분 불가 → 환경 정확성이 최우선.

**4-2. 학습**
- 검증된 렌주룰 환경 + Phase 3 파이프라인으로 self-play 0부터 학습
- 보드가 커서 상태·행동 공간이 크게 늘어남 → 충분한 self-play 판수·Colab GPU 필요 (성능 문제가 아니라 시간·자원 문제)

**4-3. 시연/확장**
- 세대 간 토너먼트로 실력 정량화 (Elo 추정)
- 학습 곡선·세대별 기보 시각화
- 학습 모델을 웹(JS)에 연결해 사람이 직접 대국하는 데모
- 하이퍼파라미터 튜닝, 병렬 self-play 등 학습 가속

**산출물**: `env/renju_rules.py` + 단위 테스트, 렌주룰 환경, 학습된 15×15 렌주 모델, 데모

---

## 4. 성장 시연을 위한 측정 설계 (전 단계 공통)

- **고정 기준 상대 대비 승률 곡선**: 학습 X판마다 랜덤/규칙 봇과 평가전 → 승률 기록 → 우상향 그래프
- **세대 간 대결**: N판 학습 모델 vs 2N판 학습 모델 → 후자 승률
- **정성적 기록**: 특정 학습량 시점의 기보 저장 → "이 수를 둘 줄 알게 된 시점" 추적

---

## 5. 권장 디렉터리 구조

```
omok-rl/
├── env/
│   ├── gomoku.py          # 환경 (board_size, n_in_row, renju 옵션)
│   └── renju_rules.py     # Phase 4: 금수(33/44/장목) 판정 + 단위 테스트
├── agents/
│   ├── random_agent.py    # 평가용 고정 상대
│   ├── q_learning.py      # Phase 1 ✅
│   ├── policy_gradient.py # Phase 2
│   └── alphazero/         # Phase 3 (Phase 4에서 그대로 재사용)
│       ├── network.py
│       ├── mcts.py
│       └── trainer.py
├── eval/
│   └── arena.py           # 승률 측정·세대 대결
├── viz/
│   └── plot_winrate.py    # 성장 곡선 시각화
├── checkpoints/
├── scripts/
└── README.md
```

---

## 6. 현실적 유의사항

- **작은 자유룰부터, 렌주룰은 마지막**: Phase 1~3은 단순 룰로 "학습이 실제로 일어나는가"를 깨끗이 검증. 렌주룰을 먼저 넣으면 환경 복잡도 + 학습 난이도가 동시에 올라가 디버깅이 지옥이 됨.
- **렌주룰의 리스크는 학습 성능이 아니라 환경 정확성**: 금수 판정(특히 33)이 까다로움. 별도 모듈 + 단위 테스트 우선으로 환경을 먼저 검증한 뒤 self-play를 붙인다.
- **렌주룰 모델은 0부터 재학습**: Phase 3 파이프라인은 재사용하되 가중치를 잇지 않는다.
- **자원**: 15×15 렌주 학습은 self-play 수만~수십만 판 + GPU(Colab) 권장. 성능이 아니라 시간·자원 이슈.
- **2인 zero-sum 처리**: 상대 시점 보상 부호 반전 등 1인 게임 RL과 다른 부분을 명확히.
